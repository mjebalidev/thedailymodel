# Public URL of the FastAPI backend (Coolify / Hetzner).
# Leave EMPTY for local dev — Vite proxies /api to http://127.0.0.1:8000.
# On Vercel, set this in Project Settings → Environment Variables, e.g.:
#   VITE_API_BASE_URL=https://ai-news-api.your-domain.com
VITE_API_BASE_URL=
